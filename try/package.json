{
  "name": "guida-try",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "prestart": "esbuild app.js --bundle --platform=browser --outfile=public/app.js",
    "start": "./server.js"
  },
  "dependencies": {
    "elm": "^0.19.2-0",
    "elm-test": "^0.19.2-0",
    "esbuild": "^0.28.0",
    "express": "^5.2.1",
    "guida": "file:..",
    "indexeddb-fs": "^2.1.5"
  }
}
