{
  "name": "guida-try",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "prestart": "esbuild app.js --bundle --platform=browser --outfile=public/app.js",
    "start": "./server.js"
  },
  "dependencies": {
    "esbuild": "^0.25.1",
    "express": "^4.22.1",
    "guida": "file:.."
  }
}