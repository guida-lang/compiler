{
  "name": "guida-try",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "prestart": "esbuild app.js --bundle --platform=browser --outfile=public/app.js",
    "start": "./server.js"
  },
  "dependencies": {
    "esbuild": "^0.28.0",
    "express": "^5.2.1",
    "guida": "file:..",
    "indexeddb-fs": "^2.1.5"
  }
}
